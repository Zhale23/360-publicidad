# Copy to .env and fill with your SMTP settings (SMTP2GO example)
# SMTP2GO recommended settings:
# SMTP_HOST=smtp.smtp2go.com
# SMTP_PORT=587
# SMTP_USER=your_smtp2go_username
# SMTP_PASS=your_smtp2go_password
# SMTP_USE_TLS=true

# Opcional: dirección FROM que aparecerá como remitente
# FROM_EMAIL=no-reply@tu-dominio.com

# Destinatario final de todos los mensajes del formulario
# TARGET_EMAIL=example@example.com

# Opcional: puerto para el servidor Flask (por defecto 5000)
# PORT=5000

# Brevo (Sendinblue) API key (optional) - if set, the server will use the Brevo HTTP API instead of SMTP
# BREVO_API_KEY=your_brevo_api_key_here
